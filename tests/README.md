# Tests
### Requirements
The tests are written using PyTest
```shell script
pip install pytest
```

### How to run them
To run the following tests, from the root folder of the project run:
```shell script
python -m pytest
```